<nav class="header-nav shadow-sm w-100">
    <div class="container h-100 d-flex justify-content-between align-items-center">
        
        <div class="d-flex align-items-center">
            <a class="text-white fw-bold d-flex align-items-center m-0 p-0 text-decoration-none me-4" href="">
                <i class="bi bi-cpu text-primary me-2" style="font-size: 1.5rem;"></i>
                <span style="font-size: 1.25rem; letter-spacing: 0.5px;">UniLab Manager</span>
            </a>

            <div class="d-flex align-items-center gap-3">
                <NavLink class="custom-btn" href="" Match="NavLinkMatch.All">
                    Головна
                </NavLink>
                <NavLink class="custom-btn" href="laboratories">
                    Лабораторії
                </NavLink>
                <NavLink class="custom-btn" href="computers">
                    Комп'ютери
                </NavLink>
            </div>
        </div>
        
        <div class="position-relative">
            
            <div class="bg-primary text-white rounded-circle d-flex justify-content-center align-items-center fw-bold shadow-sm avatar-btn" 
                 style="width: 42px; height: 42px; font-size: 1.1rem; cursor: pointer; user-select: none;"
                 @onclick="ToggleUserMenu">
                А
            </div>
            
            @if (isMenuOpen)
            {
                <div class="dropdown-overlay" @onclick="CloseUserMenu"></div>

                <div class="dropdown-menu show shadow-lg mt-2" 
                     style="position: absolute; top: 100%; right: 0; min-width: 200px; border-radius: 12px; border: none; z-index: 1050;">
                    
                    <div class="px-3 py-2 text-muted" style="font-size: 0.85rem;">
                        Увійшли як<br><strong class="text-dark">admin@unilab.edu</strong>
                    </div>
                    <hr class="dropdown-divider">
                    
                    <a class="dropdown-item py-2 px-3 custom-dropdown" href="#" @onclick="CloseUserMenu">
                        <i class="bi bi-person me-2"></i> Мій профіль
                    </a>
                    <a class="dropdown-item py-2 px-3 custom-dropdown" href="/settings" @onclick="CloseUserMenu">
                        <i class="bi bi-gear me-2"></i> Налаштування
                    </a>
                    
                    <hr class="dropdown-divider">
                    <a class="dropdown-item py-2 px-3 text-danger custom-dropdown" href="#" @onclick="CloseUserMenu">
                        <i class="bi bi-box-arrow-right me-2"></i> Вийти
                    </a>
                </div>
            }
        </div>

    </div>
</nav>
@code {
    private bool isMenuOpen = false;
    
    private void ToggleUserMenu()
    {
        isMenuOpen = !isMenuOpen;
    }

    private void CloseUserMenu()
    {
        isMenuOpen = false;
    }
}